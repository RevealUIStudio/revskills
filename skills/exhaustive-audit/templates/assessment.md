# Phase 0 assessment

Per-repo honesty. This file is **not** line coverage. Completeness of the
tree is still `coverage-status.js --mode code` exit 0.

Rubric for every repo in scope:

| Field | Meaning |
|-------|---------|
| Identity | What the tree is, from package.json / Cargo.toml / entrypoints |
| Ship state | Branch, pin, dirty, test vs main lag |
| What works | Runtime surfaces that exist in code |
| Honesty gaps | README / marketing vs code |
| Dead weight | Unused trees, dual paths, leftover dirs |
| Verdict | shippable-core / sold-ahead-of-runtime / infra-only / archive-grade / leftover |

Do not mark this assessment complete because inventory exists. Inventory is substrate.

## Repos

<!-- one section per repo; fill during Phase 0 -->
